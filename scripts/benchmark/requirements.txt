# Benchmark dependencies — install into your active Python environment:
#   pip install -r scripts/benchmark/requirements.txt
#
# On WSL2 (for LiteRT benchmarks):  uses the sidecar's .venv is fine, just add httpx/psutil
# On Windows (for llamafile benchmarks):  standard Python 3.10+ with pip

websockets>=12.0        # LiteRT WebSocket adapter
httpx>=0.27.0           # llamafile HTTP/SSE adapter
psutil>=5.9.0           # RSS memory polling + system info
pillow>=10.0.0          # test image generation (optional; falls back to minimal PNG)

# Optional — NVIDIA GPU utilisation monitoring
# pynvml>=11.0.0
