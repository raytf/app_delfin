# Benchmark dependencies for the self-contained harness venv.
#
# `npm run benchmark:*` (scripts/run-benchmark.mjs) auto-creates and installs
# scripts/benchmark/.venv from this file on first run — no manual setup needed.
# To provision or refresh it by hand: python -m venv scripts/benchmark/.venv
# then scripts/benchmark/.venv/bin/pip install -r scripts/benchmark/requirements.txt
# Requires Python 3.10+.

websockets>=12.0        # LiteRT WebSocket adapter
httpx>=0.27.0           # llamafile HTTP/SSE adapter
psutil>=5.9.0           # RSS memory polling + system info
pillow>=10.0.0          # test image generation (optional; falls back to minimal PNG)

# Optional — NVIDIA GPU utilisation monitoring
# pynvml>=11.0.0
