// TODO (Phase 3): Zustand store for chat session state.
// Holds: messages (ChatMessage[]), streaming token buffer, structured response.

export const useSessionStore = (): Record<string, never> => ({})
