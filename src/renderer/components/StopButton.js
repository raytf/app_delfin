import { jsx as _jsx } from "react/jsx-runtime";
// TODO (Phase 3): Button that sends sidecar:interrupt to stop an in-progress
// inference stream.
export default function StopButton() {
    return _jsx("div", {});
}
