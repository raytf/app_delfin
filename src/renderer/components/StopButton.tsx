// TODO (Phase 3): Button that sends sidecar:interrupt to stop an in-progress
// inference stream.

export default function StopButton(): React.ReactElement {
  return <div>{/* Placeholder — implemented in Phase 3 */}</div>
}
