// TODO (Phase 3): Show sidecar connection status dot (connected / disconnected)
// and model/backend info from SidecarStatus.

export default function StatusIndicator() {
  return <div>{/* Placeholder — implemented in Phase 3 */}</div>
}
