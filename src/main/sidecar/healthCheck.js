// TODO (Phase 2): Poll GET /health on the sidecar and forward SidecarStatus
// to the renderer via sidecar:status IPC.
export function startHealthPolling() {
    // Placeholder — implemented in Phase 2
}
export function stopHealthPolling() {
    // Placeholder — implemented in Phase 2
}
