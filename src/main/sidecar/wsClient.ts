// TODO (Phase 2): WebSocket client that connects to the Python sidecar.
// Manages connection lifecycle, sends WsOutboundMessage, forwards inbound
// messages to the renderer via IPC.

export function connectToSidecar(): void {
  // Placeholder — implemented in Phase 2
}

export function disconnectFromSidecar(): void {
  // Placeholder — implemented in Phase 2
}
