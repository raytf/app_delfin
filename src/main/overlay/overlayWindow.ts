// TODO (Phase 2): Create the sidebar overlay BrowserWindow.
// 420px wide, always-on-top, transparent chrome, positioned on the right edge.

export function createOverlayWindow(): void {
  // Placeholder — implemented in Phase 2
}
