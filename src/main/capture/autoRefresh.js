// TODO (Phase 5): Auto-refresh capture on a configurable interval with
// rolling-hash change detection and debounce.
export function startAutoRefresh(_intervalMs, _onCapture) {
    // Placeholder — implemented in Phase 5
}
export function stopAutoRefresh() {
    // Placeholder — implemented in Phase 5
}
