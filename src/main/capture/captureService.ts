// TODO (Phase 2): Implement screenshot capture using Electron desktopCapturer.
// Will capture the foreground window and return a CaptureFrame.

export function captureScreen(): Promise<void> {
  // Placeholder — implemented in Phase 2
  return Promise.resolve()
}
