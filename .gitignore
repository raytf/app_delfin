node_modules/
dist/
out/
.env
__pycache__/
*.pyc
.venv/
*.egg-info/
.DS_Store
*.tsbuildinfo

# PyInstaller build artifacts (generated by `npm run build:sidecar`)
sidecar/build/
# sidecar/dist/ is already covered by the root `dist/` pattern above

# Partial download files from `npm run download:models`
*.part

# Log files
*.log

# Windows system files
Thumbs.db
desktop.ini

# macOS
*.profraw
default.profraw

# Kokoro TTS model files — large binaries, downloaded via `npm run download:models`
sidecar/*.onnx
sidecar/*.bin