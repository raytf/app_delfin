node_modules/
dist/
dist/electron/
dist/sidecar/
dist/installers/
.env
__pycache__/
*.pyc
.venv/
*.egg-info/
.DS_Store
*.tsbuildinfo
.claude

default.profraw

# Kokoro TTS model files — large binaries, downloaded via `npm run download:models`
sidecar/*.onnx
sidecar/*.bin

# llamafile binary and GGUF model — downloaded via `npm run setup:llamafile`
llamafile/bin/
llamafile/models/

# LiteRT C++ bridge binaries and local model files — built/downloaded outside git
bin/
models/
native/litert-cpp-bridge/build/

# Benchmark results — written at runtime, not committed
results/*
!results/.gitkeep

# Local LiteRT-LM checkout for source bridge builds
litert-cpp-bridge/deps/LiteRT-LM/
